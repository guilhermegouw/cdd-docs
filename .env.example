# CDD Docs Agent Configuration

# Required: LLM API key or auth token
LLM_API_KEY=your-api-key-here

# LLM Provider (Anthropic-compatible endpoint)
# Default is Minimax M2, but can use any Anthropic-compatible API
LLM_BASE_URL=https://api.minimax.io/anthropic
LLM_MODEL=MiniMax-M2
# LLM_TIMEOUT=300.0

# Optional: Path to CDD docs (defaults to ~/code/cdd/docs)
# CDD_DOCS_PATH=/path/to/cdd/docs

# Optional: Path to vector database (defaults to ./data/vectordb)
# VECTOR_DB_PATH=./data/vectordb

# Optional: Embedding model (defaults to all-mpnet-base-v2)
# EMBEDDING_MODEL=all-mpnet-base-v2

# Optional: RAG settings (header-based chunking)
# MIN_CHUNK_SIZE=100  # sections smaller than this (in words) are skipped
# MAX_SECTION_SIZE=1000  # warn if section exceeds this many words
# TOP_K=7

# Optional: Conversation History & Query Rewriting
# MAX_HISTORY_TURNS=10
# ENABLE_QUERY_REWRITING=true
# REWRITER_MAX_TOKENS=150
# REWRITER_TEMPERATURE=0.3
